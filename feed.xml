<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://northmanpku.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://northmanpku.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-23T00:55:45+00:00</updated><id>https://northmanpku.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">How the “Correct” Way to Align CUDA Pointers Can Slow Down Shared-Memory Loads</title><link href="https://northmanpku.github.io/blog/2026/cuda-pointer-alignment/" rel="alternate" type="text/html" title="How the “Correct” Way to Align CUDA Pointers Can Slow Down Shared-Memory Loads"/><published>2026-07-05T19:26:00+00:00</published><updated>2026-07-05T19:26:00+00:00</updated><id>https://northmanpku.github.io/blog/2026/cuda-pointer-alignment</id><content type="html" xml:base="https://northmanpku.github.io/blog/2026/cuda-pointer-alignment/"><![CDATA[<h2 id="1-the-problem-pattern-tldr">1. The Problem Pattern: TL;DR</h2> <p>If your CUDA kernel partitions regions within dynamic shared memory and aligns them to 128, 512, or 1,024 bytes, a natural implementation is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>shared pointer
-&gt; convert to integer
-&gt; round up with a bit mask
-&gt; convert back to pointer
</code></pre></div></div> <p>This pattern is common in CPU code, and the resulting address is numerically correct in CUDA as well.</p> <p>The problem is that the CUDA compiler cares about more than the numerical value of an address. It must also determine which address space a pointer belongs to. If the final pointer is derived from an <code class="language-plaintext highlighter-rouge">extern __shared__</code> array through ordinary pointer arithmetic, the compiler can usually prove that it points to shared memory. An ordinary C++ dereference can then be compiled into a shared-memory instruction.</p> <p>If the pointer is converted to an integer and later reconstructed from that integer, however, the compiler’s dataflow chain is broken. We know that the address still lies in shared memory, but the compiler may not. With less information available, it uses more conservative—and less efficient—instructions, causing an unnecessary performance loss.</p> <p>A more robust pattern is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>shared pointer
-&gt; use integer arithmetic only to compute misalignment / padding
-&gt; return original pointer + padding
</code></pre></div></div> <p>In other words, use the integer only to calculate how many more bytes are needed for alignment. The final result remains a pointer-arithmetic descendant of the original shared-memory pointer.</p> <h2 id="2-minimal-reproduction">2. Minimal Reproduction</h2> <p>We can reproduce the issue with a deliberately simple kernel. The example contains two kernels that do the same work:</p> <ul> <li>Take a tile buffer from dynamic shared memory.</li> <li>Manually align the tile base.</li> <li>Repeatedly perform vectorized shared-memory loads in a hot loop, with a small amount of dependent computation.</li> </ul> <p>The overall flow (the details are not important here) is:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">static</span> <span class="k">constexpr</span> <span class="kt">int</span> <span class="n">kSkew</span> <span class="o">=</span> <span class="mi">37</span><span class="p">;</span>

<span class="k">extern</span> <span class="s">"C"</span> <span class="n">__global__</span> <span class="nf">__launch_bounds__</span><span class="p">(</span><span class="n">kThreads</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span> <span class="kt">void</span>
<span class="n">roundtrip_bad_kernel</span><span class="p">(</span><span class="n">uint4</span> <span class="o">*</span><span class="n">out</span><span class="p">,</span> <span class="kt">uint32_t</span> <span class="n">seed</span><span class="p">,</span> <span class="kt">int</span> <span class="n">iters</span><span class="p">)</span> <span class="p">{</span> <span class="c1">// or roundtrip_good_kernel</span>
  <span class="k">extern</span> <span class="n">__shared__</span> <span class="kt">char</span> <span class="n">smem</span><span class="p">[];</span>
  <span class="kt">char</span> <span class="o">*</span><span class="n">base</span> <span class="o">=</span> <span class="n">bad_aligned_base</span><span class="p">(</span><span class="n">smem</span> <span class="o">+</span> <span class="n">kSkew</span><span class="p">);</span> <span class="c1">// or good_aligned_base</span>
  <span class="n">uint4</span> <span class="o">*</span><span class="n">tile</span> <span class="o">=</span> <span class="k">reinterpret_cast</span><span class="o">&lt;</span><span class="n">uint4</span> <span class="o">*&gt;</span><span class="p">(</span><span class="n">base</span><span class="p">);</span>

  <span class="n">init_shared</span><span class="p">(</span><span class="n">tile</span><span class="p">,</span> <span class="n">seed</span><span class="p">);</span>
  <span class="n">uint4</span> <span class="n">acc</span> <span class="o">=</span> <span class="n">load_loop</span><span class="p">(</span><span class="n">tile</span><span class="p">,</span> <span class="n">iters</span><span class="p">,</span> <span class="n">seed</span><span class="p">);</span>
  <span class="n">out</span><span class="p">[</span><span class="n">blockIdx</span><span class="p">.</span><span class="n">x</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span> <span class="o">+</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span><span class="p">]</span> <span class="o">=</span> <span class="n">acc</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p>Both kernels use the same load logic (also not important here):</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">__device__</span> <span class="n">__forceinline__</span> <span class="n">uint4</span> <span class="nf">load_loop</span><span class="p">(</span><span class="n">uint4</span> <span class="k">const</span> <span class="o">*</span><span class="n">tile</span><span class="p">,</span> <span class="kt">int</span> <span class="n">iters</span><span class="p">,</span>
                                           <span class="kt">uint32_t</span> <span class="n">seed</span><span class="p">)</span> <span class="p">{</span>
  <span class="n">uint4</span> <span class="n">acc</span> <span class="o">=</span> <span class="n">make_uint4</span><span class="p">(</span><span class="n">seed</span> <span class="o">+</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span><span class="p">,</span> <span class="n">seed</span> <span class="o">^</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span><span class="p">,</span>
                         <span class="n">seed</span> <span class="o">+</span> <span class="mi">3u</span> <span class="o">*</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span><span class="p">,</span> <span class="n">seed</span> <span class="o">^</span> <span class="mh">0x5f3759dfu</span><span class="p">);</span>

<span class="cp">#pragma unroll 1
</span>  <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">r</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">r</span> <span class="o">&lt;</span> <span class="n">iters</span><span class="p">;</span> <span class="o">++</span><span class="n">r</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">int</span> <span class="k">const</span> <span class="n">idx</span> <span class="o">=</span> <span class="p">(</span><span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span> <span class="o">+</span> <span class="n">r</span><span class="p">)</span> <span class="o">&amp;</span> <span class="p">(</span><span class="n">kVecCount</span> <span class="o">-</span> <span class="mi">1</span><span class="p">);</span>
    <span class="n">uint4</span> <span class="n">v</span> <span class="o">=</span> <span class="n">tile</span><span class="p">[</span><span class="n">idx</span><span class="p">];</span>
    <span class="n">acc</span><span class="p">.</span><span class="n">x</span> <span class="o">+=</span> <span class="n">v</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>
    <span class="n">acc</span><span class="p">.</span><span class="n">y</span> <span class="o">^=</span> <span class="n">v</span><span class="p">.</span><span class="n">y</span><span class="p">;</span>
    <span class="n">acc</span><span class="p">.</span><span class="n">z</span> <span class="o">+=</span> <span class="n">v</span><span class="p">.</span><span class="n">z</span><span class="p">;</span>
    <span class="n">acc</span><span class="p">.</span><span class="n">w</span> <span class="o">^=</span> <span class="n">v</span><span class="p">.</span><span class="n">w</span><span class="p">;</span>
  <span class="p">}</span>

  <span class="k">return</span> <span class="n">acc</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p>The only difference is how the pointer is aligned (this is the important part):</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">static</span> <span class="k">constexpr</span> <span class="kt">uintptr_t</span> <span class="n">kAlign</span> <span class="o">=</span> <span class="mi">1024</span><span class="p">;</span>

<span class="n">__device__</span> <span class="n">__forceinline__</span> <span class="kt">char</span> <span class="o">*</span><span class="nf">bad_aligned_base</span><span class="p">(</span><span class="kt">char</span> <span class="o">*</span><span class="n">p</span><span class="p">)</span> <span class="p">{</span>
  <span class="kt">uintptr_t</span> <span class="n">raw</span> <span class="o">=</span> <span class="k">reinterpret_cast</span><span class="o">&lt;</span><span class="kt">uintptr_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">p</span><span class="p">);</span> <span class="c1">// first convert to an integer</span>
  <span class="n">raw</span> <span class="o">=</span> <span class="p">(</span><span class="n">raw</span> <span class="o">+</span> <span class="n">kAlign</span> <span class="o">-</span> <span class="mi">1</span><span class="p">)</span> <span class="o">&amp;</span> <span class="o">~</span><span class="p">(</span><span class="n">kAlign</span> <span class="o">-</span> <span class="mi">1</span><span class="p">);</span>        <span class="c1">// align</span>
  <span class="k">return</span> <span class="k">reinterpret_cast</span><span class="o">&lt;</span><span class="kt">char</span> <span class="o">*&gt;</span><span class="p">(</span><span class="n">raw</span><span class="p">);</span>           <span class="c1">// convert back to a pointer</span>
<span class="p">}</span>

<span class="n">__device__</span> <span class="n">__forceinline__</span> <span class="kt">char</span> <span class="o">*</span><span class="nf">good_aligned_base</span><span class="p">(</span><span class="kt">char</span> <span class="o">*</span><span class="n">p</span><span class="p">)</span> <span class="p">{</span>
  <span class="kt">uintptr_t</span> <span class="k">const</span> <span class="n">mis</span> <span class="o">=</span> <span class="k">reinterpret_cast</span><span class="o">&lt;</span><span class="kt">uintptr_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">p</span><span class="p">)</span> <span class="o">&amp;</span> <span class="p">(</span><span class="n">kAlign</span> <span class="o">-</span> <span class="mi">1</span><span class="p">);</span>
  <span class="c1">// Keep p itself as a shared-memory pointer and add only the padding offset.</span>
  <span class="k">return</span> <span class="n">p</span> <span class="o">+</span> <span class="p">((</span><span class="n">kAlign</span> <span class="o">-</span> <span class="n">mis</span><span class="p">)</span> <span class="o">&amp;</span> <span class="p">(</span><span class="n">kAlign</span> <span class="o">-</span> <span class="mi">1</span><span class="p">));</span>
<span class="p">}</span>
</code></pre></div></div> <p>On a B200, the SASS summaries for the two implementations are:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>=== roundtrip_bad_kernel ===
LD.E.128: 1
LDS.128 : 0
ST.E.128: 5
STS.128 : 0

=== roundtrip_good_kernel ===
LD.E.128: 0
LDS.128 : 1
ST.E.128: 0
STS.128 : 5
</code></pre></div></div> <p>The relevant SASS instructions are:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>// bad: generic load
/*0870*/ LD.E.128 R8, desc[UR8][R2.64] ;

// good: shared-memory load
/*0950*/ LDS.128 R8, [R0+UR4+0x25] ;
</code></pre></div></div> <p>In the same benchmark run:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>bad  per_launch=314.396 us
good per_launch=270.530 us
ratio=1.162x
identical checksums
</code></pre></div></div> <p>The numerical shared-memory address is the same, and the computation produces the same result. Yet the difference in pointer provenance alone changes the SASS from <code class="language-plaintext highlighter-rouge">LDS</code>/<code class="language-plaintext highlighter-rouge">STS</code> to <code class="language-plaintext highlighter-rouge">LD.E</code>/<code class="language-plaintext highlighter-rouge">ST.E</code>, and performance changes with it.</p> <p>In the real performance-debugging scenario where I encountered this issue, the kernel runtime differed by as much as 1.5× between the two cases.</p> <h2 id="3-a-deeper-analysis">3. A Deeper Analysis</h2> <p>An ordinary pointer in CUDA C++ is a high-level-language pointer. At the PTX/SASS level, memory is divided into more explicit state spaces, including global, shared, local, constant, and generic.</p> <p>The same piece of shared memory can have two address representations at different levels. One is the generic pointer used by ordinary CUDA C++ code. It is typically 64 bits wide, can be printed with <code class="language-plaintext highlighter-rouge">%p</code>, stored in a <code class="language-plaintext highlighter-rouge">uintptr_t</code>, and passed around as an ordinary pointer. The other is an address in PTX’s <code class="language-plaintext highlighter-rouge">.shared</code> state space. It is more like an offset within the current cooperative thread array’s (CTA’s) shared-memory window and is typically a shorter raw offset. A generic address provides a unified representation that can map through different windows to global, shared, or local memory.</p> <p>In our minimal kernel, for example, printing the start of dynamic shared memory with <code class="language-plaintext highlighter-rouge">printf("%p", smem)</code> in C++ produces a 64-bit generic pointer such as <code class="language-plaintext highlighter-rouge">0x7b4400000400</code>, while <code class="language-plaintext highlighter-rouge">__cvta_generic_to_shared(smem)</code> produces a raw shared offset such as <code class="language-plaintext highlighter-rouge">0x400</code>. Both refer to the same piece of shared memory, but they are not the same address representation. The high bits in <code class="language-plaintext highlighter-rouge">0x7b4400000400</code> can be understood as the encoding of the shared-memory window within the generic address space for that run, whereas <code class="language-plaintext highlighter-rouge">0x400</code> is the offset within the shared-memory window.</p> <p>The bad and good paths can produce exactly the same runtime pointer value, and both can convert back to the same shared offset. The real difference is whether the compiler still knows that the address belongs to the <code class="language-plaintext highlighter-rouge">.shared</code> state space when lowering the code to PTX.</p> <h3 id="the-paths-have-already-diverged-in-ptx">The Paths Have Already Diverged in PTX</h3> <p>At the beginning of the PTX file, dynamic shared memory is declared as a <code class="language-plaintext highlighter-rouge">.shared</code> symbol. This CUDA declaration:</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">extern</span> <span class="n">__shared__</span> <span class="kt">char</span> <span class="n">smem</span><span class="p">[];</span>
</code></pre></div></div> <p>produces a PTX declaration like this:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>.extern .shared .align 16 .b8 smem[];
</code></pre></div></div> <p>The key PTX pattern for the bad implementation is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mov.u32         %r63, smem;        // get the raw shared address of the PTX .shared symbol
cvt.u64.u32     %rd7, %r63;
cvta.shared.u64 %rd8, %rd7;        // shared raw address -&gt; generic address
add.s64         %rd9, %rd8, 1060;  // add an offset to the generic 64-bit address
and.b64         %rd1, %rd9, -1024; // align the generic 64-bit address to 1024 bytes
...
add.s64         %rd21, %rd1, %rd20;
ld.v4.u32       {%r109, %r110, %r111, %r112}, [%rd21]; // generic PTX load
</code></pre></div></div> <p>Notice that the final instruction is <code class="language-plaintext highlighter-rouge">ld.v4.u32</code>, with no <code class="language-plaintext highlighter-rouge">.shared</code> qualifier. The bad implementation is therefore already a generic load at the PTX level, and <code class="language-plaintext highlighter-rouge">ptxas</code> later lowers it to the SASS instruction <code class="language-plaintext highlighter-rouge">LD.E.128</code>.</p> <p>The key PTX pattern for the good implementation is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mov.u32           %r121, smem;        // get the raw shared address of the PTX .shared symbol
...
and.b32           %r124, %r123, 1023;
add.s32           %r47, %r121, %r124; // compute padding on a shared 32-bit address expression
...
ld.shared.v4.u32  {%r128, %r129, %r130, %r131}, [%r127+37]; // shared PTX load
</code></pre></div></div> <p>Here, the final instruction is <code class="language-plaintext highlighter-rouge">ld.shared.v4.u32</code>. The good implementation remains a shared-memory load at the PTX level, and <code class="language-plaintext highlighter-rouge">ptxas</code> later lowers it to the SASS instruction <code class="language-plaintext highlighter-rouge">LDS.128</code>. The runtime pointer values of the bad and good implementations can therefore be identical even though their intermediate code generation has already diverged.</p> <h3 id="lds128-vs-lde128"><code class="language-plaintext highlighter-rouge">LDS.128</code> vs. <code class="language-plaintext highlighter-rouge">LD.E.128</code></h3> <p><code class="language-plaintext highlighter-rouge">LDS.128</code> is a 128-bit load from the shared-memory window. It uses the dedicated shared-memory path, has a shorter address representation, and tells the scheduler explicitly that this is a shared-memory load.</p> <p><code class="language-plaintext highlighter-rouge">LD.E.128</code> is a generic load. In the generated SASS, it commonly uses a 64-bit address and a memory descriptor, for example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>LD.E.128 ..., desc[...][...64]
</code></pre></div></div> <p>Even when the generic address ultimately refers to shared memory, the compiler and scheduler must handle it more conservatively. In a hot loop dominated by shared-memory loads, this directly slows down the producer.</p> <h2 id="4-conclusion">4. Conclusion</h2> <p>When accessing shared memory through ordinary C++ dereferences, try to keep the pointer as a direct pointer-arithmetic descendant of an <code class="language-plaintext highlighter-rouge">extern __shared__</code> or <code class="language-plaintext highlighter-rouge">__shared__</code> object.</p> <p>Integer arithmetic is fine for calculating alignment padding, but do not reconstruct the final pointer by reinterpreting the integer. Return the original pointer plus the padding instead.</p> <p>For hot loops, do not inspect only the CUDA source. Check the actual PTX and SASS as well. In particular, when correctness looks fine but performance does not, SASS is often more honest than the source code.</p> <h2 id="references">References</h2> <ul> <li><a href="https://docs.nvidia.com/cuda/cuda-c-programming-guide/">NVIDIA CUDA C++ Programming Guide: Address Space Conversion Functions</a></li> <li><a href="https://docs.nvidia.com/cuda/parallel-thread-execution/">NVIDIA PTX ISA: <code class="language-plaintext highlighter-rouge">ld</code> and State-Space Addressing</a></li> <li><a href="https://docs.nvidia.com/cuda/ptx-writers-guide-to-interoperability/index.html">NVIDIA PTX Writer’s Guide to Interoperability: Pointer Sizes vs. Shared/Local Address Sizes</a></li> <li><a href="https://docs.nvidia.com/cuda/archive/12.6.0/cuda-binary-utilities/index.html">NVIDIA CUDA Binary Utilities: SASS Instruction Classes</a></li> <li><a href="https://forums.developer.nvidia.com/t/generic-pointer-vs-shared-memory-pointer/62546">NVIDIA Developer Forums: Generic Pointer vs. Shared-Memory Pointer</a></li> <li><a href="https://forums.developer.nvidia.com/t/why-do-i-need-to-convert-a-pointer-to-shared-address-space-before-using-the-ldmatrix-instruction/274466">NVIDIA Developer Forums: Why <code class="language-plaintext highlighter-rouge">ldmatrix</code> Requires an Address in Shared Space</a></li> </ul>]]></content><author><name></name></author><category term="Technology"/><category term="CUDA"/><category term="MLsys"/><summary type="html"><![CDATA[A pointer-to-integer round trip can erase CUDA shared-memory provenance, turn LDS/STS into generic LD.E/ST.E instructions, and slow down an otherwise correct kernel.]]></summary></entry><entry><title type="html">The Catastrophe of #pragma unroll in CUDA Programming</title><link href="https://northmanpku.github.io/blog/2025/unroll/" rel="alternate" type="text/html" title="The Catastrophe of #pragma unroll in CUDA Programming"/><published>2025-09-07T23:10:00+00:00</published><updated>2025-09-07T23:10:00+00:00</updated><id>https://northmanpku.github.io/blog/2025/unroll</id><content type="html" xml:base="https://northmanpku.github.io/blog/2025/unroll/"><![CDATA[<p>This post records a CUDA performance issue I previously overlooked, as a reminder to myself and to friends who might encounter similar situations in the future.</p> <p>Recently, I’ve been optimizing the performance of the Linear kernel in the Mirage Persistent Kernel. In short, this kernel, as part of the overall persistent kernel, is responsible for performing matrix multiplication and residual addition using 128 threads in a single thread block on an Ampere machine. In the old version of the code, I swapped loop orders to reduce loading, checked for and reduced bank conflicts, cut down unnecessary shared memory usage, implemented a global pipeline, vectorized write-backs, added branch avoidance (this one is interesting enough for another write-up), and so on. After all these aggressive changes, I did see performance improvements in test scenarios. But then, when I tested on small baseline cases, I found performance dropped by 2×!</p> <p>This result left me completely baffled. The matrix shape (8, 4096) × (4096, 64) is heavily used in our qwen3 demo, and a 2× slowdown is absolutely unacceptable. What was most confusing was that my optimizations should have had little effect in this smaller OUTPUT_SIZE (64) setting. In many respects, the new and old versions should have similar memory access and computation patterns, with the new version even having objective advantages like reduced resource usage and fewer bank conflicts. There was no reason it should be 2× slower.</p> <p>Fortunately, thanks to previous experience, I was already somewhat familiar with NCU, so I quickly ran extensive comparisons to look for clues. Out of all the experiments, two related findings stood out:</p> <ol> <li> <p>If I quadrupled the computation per iteration, performance flipped from being 2× slower than the old version to actually being faster.</p> </li> <li> <p>One particular metric skyrocketed in the new version: Stall No Instruction.</p> </li> </ol> <p><img src="/assets/img/blogs/unroll/p1.png" alt="NCU profiling showing dramatic increase in Stall No Instruction metric" class="blog-fig"/> <em>Figure 1: NCU profiling comparison showing the dramatic increase in “Stall No Instruction” metric between old and new kernel versions.</em></p> <p>Compared to the green bar of the old version, the new version’s number was over 50× higher! Sure enough, I found an important clue in the official documentation during my search:</p> <p><img src="/assets/img/blogs/unroll/p2.png" alt="NVIDIA documentation excerpt about pragma unroll and instruction cache" class="blog-fig"/></p> <p><code class="language-plaintext highlighter-rouge">#pragma unroll</code> — in our code, whenever a for loop had a range and step size known at compile time, we applied <code class="language-plaintext highlighter-rouge">#pragma unroll</code>. Let’s see what the official docs say about it:</p> <p><img src="/assets/img/blogs/unroll/p3.png" alt="NVIDIA documentation explaining pragma unroll effects on code size and instruction cache" class="blog-fig"/></p> <p>In other words, we told the compiler to fully unroll every possible loop. This nearly eliminates all loop comparison and increment overhead, reduces branching, and gives the compiler more freedom to reorder instructions. <strong>But it also massively bloats the code size—so much so that the instruction cache cannot hold it.</strong> The warp can’t fetch instructions directly from the cache and instead has to take much longer fetch paths. In small compute workloads, the computation is too short to hide this instruction fetch overhead, leading to the surge of “stall no instruction.” To verify this, I dumped the SASS files to count instructions:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cuobjdump <span class="nt">-sass</span> <span class="o">[</span><span class="nb">exec</span><span class="o">]</span> <span class="o">&gt;</span> <span class="o">[</span>output_name].sass
</code></pre></div></div> <table> <thead> <tr> <th>Version</th> <th>SASS instruction counts</th> </tr> </thead> <tbody> <tr> <td>Old version</td> <td>1.3k</td> </tr> <tr> <td>New version</td> <td>12.9k</td> </tr> </tbody> </table> <p>Bingo! The new version’s compiled SASS instruction count was 10× higher than the old version. But wait—since both versions were always using full <code class="language-plaintext highlighter-rouge">#pragma unroll</code>, why wasn’t the old version just as inefficient?</p> <p>Looking back at the code, I realized there had been a “happy accident”: in the old version, some loops only executed once, so the author hadn’t added <code class="language-plaintext highlighter-rouge">#pragma unroll</code> in those cases. That unintentionally kept the instruction count reasonable and gave the old code an edge in the new scenario. Ironically, this oversight is what drew my attention to the huge discrepancy. To test further, I added <code class="language-plaintext highlighter-rouge">#pragma unroll</code> to all loops in the old version and re-ran benchmarks. As expected, SASS instruction count and execution time all skyrocketed:</p> <table> <thead> <tr> <th>Version</th> <th>SASS instruction counts</th> </tr> </thead> <tbody> <tr> <td>Old version + full <code class="language-plaintext highlighter-rouge">#pragma unroll</code></td> <td>14k</td> </tr> </tbody> </table> <p>Given this, the improvement direction was clear: for small workloads, we must limit <code class="language-plaintext highlighter-rouge">#pragma unroll</code> expansion to strike a balance between redundant computation and instruction cache capacity. However, “balance” here is vague. Our understanding of instruction cache behavior is limited, and with a kernel containing more than a dozen loops, it’s nearly impossible to quantitatively decide the optimal unroll factors—once again proving that “the endgame of the universe is parameter tuning.” Qualitatively, we should fully unroll short, frequent loops, but keep unrolling limited for long, instruction-heavy ones. I’d love to hear more experiences and insights from others on this methodology.</p> <p>In experiments, I confirmed that small kernels are indeed very sensitive to unrolling strategies. Eventually, I found a combination that performed reasonably well within the tested range, finally escaping the nightmare of abnormal performance. There are still other optimization opportunities in this kernel worth trying, but for now, here are the main takeaways:</p> <ol> <li> <p>Excessive <code class="language-plaintext highlighter-rouge">#pragma unroll</code> causes instruction bloat, exceeding the instruction cache capacity, leading to increased fetch latency and a surge in “stall no instruction.”</p> </li> <li> <p>For small workloads, compute time is too short to hide fetch delays, so the slowdown is especially dramatic (a full 2×).</p> </li> </ol> <p>Update – 2025.09.07</p> <p>Today, I brute-forced all reasonably possible <code class="language-plaintext highlighter-rouge">#pragma unroll N</code> combinations for OUTPUT_SIZE=64 and uncovered some patterns specific to our code in this scenario. Roughly speaking: the outermost loop must be set to 2 (neither 0 nor higher—both slow down performance significantly); some parts of the code must either be fully unrolled or not unrolled at all (partial unroll makes things worse); some parts must remain unrolled; the rest can be fully unrolled. From the dizzying output table, we gained some insights, but at best this just gives us a new direction to try in future implementations. To truly maximize performance, we’ll still need to finetune the mix of unroll degrees across different loops.</p>]]></content><author><name></name></author><category term="Technology"/><category term="CUDA"/><category term="MLsys"/><summary type="html"><![CDATA[This post records a CUDA performance issue I previously overlooked, as a reminder to myself and to friends who might encounter similar situations in the future.]]></summary></entry></feed>